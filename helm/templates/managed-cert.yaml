# Certificate is now managed via Terraform (google_compute_managed_ssl_certificate)
# and referenced by the ingress via the pre-shared-cert annotation.
# This avoids per-cluster cert provisioning delays during DR failovers.
